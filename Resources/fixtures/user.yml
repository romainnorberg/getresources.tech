App\Entity\User:
    user (template):
        is_active: true
        created: <dateTime()>
        updated: <dateTime()>

    user{1..10} (extends user):
        email (unique): '<email()>'
        username: '@self->email'
        plain_password: <password()>

    user_admin (extends user):
        email: john@malkovich.com
        username: '@self->email'
        plain_password: <password()>